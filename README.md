# نظام تدقيق نقليات الدريس — نموذج التكلفة الواجبة

منصّة تدقيق فواتير نقليات توريد أرامكو: تحوّل الفواتير الشهرية (Excel) إلى تحليل
«الهدر» (محطات وُرّدت من مركز أبعد من الأقرب)، مع لوحات تحكم، خرائط مسارات،
كشف شذوذ، كفاءة حمولة، وتقرير رسمي بالتوصيات.

> أُعيدت هيكلة المشروع من ملف `index.html` واحد بحجم ~3 ميجابايت إلى مشروع
> [Vite](https://vitejs.dev/) منظّم وقابل للصيانة، مع تحميل مكتبتي
> Leaflet و SheetJS من npm بدل تضمينهما.

## التشغيل

```bash
npm install      # تثبيت المكتبات
npm run dev      # خادم تطوير محلي (إعادة تحميل فوري)
npm run build    # بناء للإنتاج → مجلد dist/
npm run preview  # معاينة ناتج البناء محليًا
```

## البنية

```
.
├── index.html          # هيكل الصفحة (login + topbar) ونقطة دخول Vite
├── vite.config.js      # إعداد البناء
├── netlify.toml        # أمر البناء + ترويسات الأمان لـ Netlify
├── public/
│   ├── favicon.svg
│   └── _redirects      # توجيه SPA على Netlify
└── src/
    ├── main.js         # منطق الواجهة: التنقل، العرض، الخريطة، رفع الفواتير، خطة العمل
    ├── engine.js       # محرّك التدقيق (runAudit): الفواتير الخام → AGG/STATIONS/GEO
    ├── data.js         # البيانات الأوّلية والمراجع الثابتة (GEO/CENTERS/RM/REF)
    └── styles/
        └── app.css     # تنسيقات التطبيق (تنسيقات Leaflet تأتي من npm)
```

### تدفّق البيانات

1. `engine.js` يصدّر `AldreesAudit.runAudit(rows, REF, period)` ويعيد `{ AGG, STATIONS, GEO_trips }`.
2. `data.js` يوفّر القيم الأوّلية (`initialAGG`, `initialStations`) والمراجع الثابتة.
3. `main.js` يربط الواجهة؛ وعند رفع فاتورة جديدة يستدعي `runAudit` ويحدّث الحالة عبر `applyAudit`.

## ملاحظات أمنية مهمّة

تسجيل الدخول الحالي **تحقّق من جهة العميل فقط** (مقارنة هاش في المتصفح)، وهو
غير كافٍ لحماية بيانات حسّاسة. يُنصح بشدّة بترقيته إلى مصادقة من خادم
(مثل Netlify Identity / Auth0) كخطوة تالية قبل النشر الإنتاجي.

## سجل الإصدارات

- **1.9.18** — اعتماد ملف المسافات المحدّث **v6.0_7** (فيه تصحيح 1626 → المدينة 260.6كم وإضافة 6 محطات جديدة): استُبدلت كل مسافات الطرق به وأُعيد توليد التدقيق — الهدر **1,917,674 ر.س**. (بصمة البيانات حُدّثت فتُلغى الفترات المخزّنة القديمة تلقائياً.)

- **1.9.17** — تصحيح محطة 1626 (الحايط): أقرب مركز لها **أرامكو المدينة (261كم)** بدل الجبيل 483كم الخاطئ (كانت مقيسة من موقع خاطئ في الملف). أظهر التصحيح هدرها الحقيقي **13,350 ر.س** (مورّدة من القصيم 403كم مع أن المدينة أقرب). الهدر الإجمالي **1,917,672**.

- **1.9.16** — إضافة زر «نظام قياس المسافات» في الشريط العلوي (الصفحة الرئيسية) يفتح `https://www.aldrees-road.com` في تبويب جديد.

- **1.9.15** — **إصلاح مهم:** الفترات المرفوعة سابقاً كانت تُخزَّن في المتصفح (localStorage) بأرقامها القديمة (مسافات/منطق قديم)، فتظهر بعد التحديث بدل إعادة حسابها. الآن عند تغيّر بصمة البيانات (المسافات/المحرّك) تُلغى الفترات المخزّنة تلقائياً — فتظهر أرقام أبريل الصحيحة (1,904,322) وتُعاد بقية الأشهر بالحساب الصحيح عند رفعها.

- **1.9.14** — اعتماد ملف المسافات المحدّث **v6.0 (فعلي فقط، 21 مركز)** كمصدر وحيد لمسافات الطرق وحذف السابق نهائياً: مصفوفة مقاسة كاملة (21/21) لكل 1369 محطة. أُعيد توليد تدقيق أبريل بالكامل منه — الهدر **1,904,322 ر.س**. صحّح محطة 1194 (أقربها **ضباء 118كم**) وبقية المحطات. أي فاتورة تُرفع لاحقاً تُدقّق بهذه المسافات.

- **1.9.13** — تصحيح 3 محطات مسافات طرقها في ملف «مقاس فعلي» كانت مقيسة من موقع خاطئ (1194 السيح، 1082 تربة، 1626 الحايط): أُعيد حساب مسافاتها من إحداثياتها الصحيحة وثُبّت مركز التوريد الفعلي من الفاتورة — فمثلاً محطة 1194 أقربها الحقيقي **أرامكو ضباء (153كم)** لا ينبع، وزال هدرها الوهمي (7,543 → 0). الهدر الإجمالي **1,860,972**. (تحقّق شامل: 11 محطة أخرى إحداثياتها خاطئة لكن مسافاتها وتدقيقها صحيح — تأثيرها على الخريطة فقط.)

- **1.9.12** — اعتماد ملف مواقع المحطات الرسمي (إحداثيات Latitude/Longitude): تحديث مواقع 1268 محطة على الخريطة من الملف المعتمد بدل الإحداثيات القديمة الخاطئة. لا يؤثّر على التدقيق (المسافات تأتي من ملف «مقاس فعلي»)، إنما يصحّح مواقع المحطات على الخريطة.

- **1.9.11** — عرض «فرق المبلغ» بقيمته الفعلية الدقيقة (بكسوره، مثل 276.575) بدل التقريب — فيتطابق **فرق المبلغ × الردود = إجمالي الفرق**، و«مبلغ الطريق» أصبح متسقاً معه (المفوتر − الفرق). مطبّق على كل الصفوف في الجدول والتصدير.

- **1.9.10** — توضيح عمودَي «فرق المبلغ» و«إجمالي الفرق» في مقارنة تكلفة النقل (تلميح): فرق المبلغ = متوسط الردة مقرّباً، وإجمالي الفرق = المجموع الفعلي الدقيق (يطابق الهدر الكلي في اللوحة) — فقد يختلف قليلاً عن حاصل ضرب المتوسط بسبب التقريب. لا تغيير في الأرقام.

- **1.9.9** — تطبيق **حد التفاوت المسموح (110 ر.س/ردة) على كل الإحصائيات**: أي مركز انطلاق فرقه للردة أقل من 110 ر.س يُعتبر مقبولاً (لا هدر) — يُخصم من الهدر الإجمالي والتصنيفات وكل اللوحات، لا من جدول تكلفة النقل فقط. الهدر الصحيح **1,890,497 ر.س** (مُعفى 574,011)، وأصبحت 855 محطة سليمة. مطبّق في محرّك التدقيق فيشمل أي فاتورة تُرفع لاحقاً (الحد قابل للتعديل عبر `REF.tolPerTrip`).

- **1.9.8** — استبعاد الصفوف قليلة الأثر من مقارنة تكلفة النقل: أي صف فرق مبلغه للردة أقل من **110 ر.س** يُزال من الجدول والتصدير (تركيز على الفروقات المؤثّرة فقط).

- **1.9.7** — اعتماد **ملف المسافات الرسمي «مقاس فعلي»** كمصدر وحيد لمسافات الطرق: مصفوفة طرق حقيقية مقاسة لكل محطة إلى 21 مركزاً (1369 محطة، +58 محطة جديدة). أُعيد توليد تدقيق أبريل بالكامل من الفاتورة الأصلية بهذه المسافات — الهدر الصحيح **2,464,508 ر.س** (بدل 2,476,214). صحّح تلقائياً كل المحطات ذات مصفوفة الطرق الخاطئة: 915 (الأقرب أرامكو أبها 338كم)، و761/1448/1664/1085 (التي كانت إحداثياتها خاطئة، الآن بأقرب مركز صحيح). أي فاتورة تُرفع لاحقاً تُدقّق بهذه المسافات الفعلية. أُلغيت التصحيحات اليدوية الفردية (الملف يغطيها).

- **1.9.6** — إكمال عمود «المركز المعتمد (أرامكو)» لكل المحطات: المحطات التي لا يوجد لها رد أرامكو رسمي (مثل 1916 الدالبة-الجوف من فاتورة يونيو) تعرض الآن **أقرب مركز مرجعي** بوسم تقديري «≈» (رمادي مائل) بدل الفراغ — مع تمييزه بوضوح عن الرد الرسمي في الجدول والتصدير. تحقّق شامل من بيانات الاعتماد: كل الـ1307 رد رسمي يشير لمركز صحيح (٠ أخطاء)، ورُصدت 4 محطات إحداثياتها خاطئة (761، 1448، 1664، 1085) مركزها المعتمد صحيح لكن موقعها يحتاج تصحيحاً (نفس فئة 915).

- **1.9.5** — اعتماد مسافة الطريق الفعلية المقاسة لمحطة 915: الأقرب/المعتمد = **أرامكو شمال جدة** بمسافة طريق فعلية **389.7كم** (بدل التقدير)، فالمفوتر (405كم) ≈ الطريق الفعلي والهدر الحقيقي **941 ر.س** فقط (فرق شريحة واحدة) بدل 5,911. العمودان «الصحيح» و«المعتمد» يتطابقان (أخضر). انخفض إجمالي الهدر بـ 16,450 ر.س عن البيانات الأصلية.

- **1.9.4** — إضافة عمود «المركز المعتمد (أرامكو)» إلى مقارنة تكلفة النقل (من ردود أرامكو الرسمية Q1-2026، 1307 محطة) بجانب «المركز الصحيح (الأقرب)» المحسوب — يظهر **أخضر إن تطابق المعتمد مع الأقرب، برتقالي إن اختلف** — وفي تصدير Excel أيضاً.

- **1.9.3** — تصحيح موقع/طرق محطة 915 (الصعاق المندق - الباحة): كانت مصفوفة طرقها منسوخة خطأً من محطة رياض فظهر «الأقرب» أرامكو جنوب الرياض على بُعد 34كم (والباحة تبعد ~755كم!) ما ضخّم الهدر إلى 17,391 ر.س. أُعيد حساب مسافات الطرق من موقعها الصحيح (خط مستقيم × معامل 1.21 المعايَر من بيانات الطرق الحقيقية) — الأقرب الآن **أرامكو أبها (307كم)** والهدر الصحيح **5,911 ر.س**. عبر وحدة تصحيحات `corrections.js` قابلة للتوسّع (تُصلح المحرّك للتقارير القادمة أيضاً). وتوضيح عنوان عمود «المركز الصحيح (الأقرب)» في مقارنة تكلفة النقل.

- **1.9.2** — تصحيح مقارنة تكلفة النقل: صفٌّ لكل (محطة × مركز انطلاق) بدل صف واحد بالمركز الأكثر ردوداً. المحطة التي تُورَّد من أكثر من مركز تظهر بكل مراكزها، وكل مركز بأرقامه (كم/مبلغ/فرق/هدر) — فيُنسب الهدر لمصدره الصحيح.

- **1.9.1** — خانة بحث في تبويب «مقارنة تكلفة النقل» للبحث عن محطة برقمها أو اسمها أو المركز أو المنتج.

- **1.9.0** — تبويب «مقارنة تكلفة النقل» في مركز التدقيق المحاسبي (بنفس تنسيق تقرير Transport Cost comparison): لكل محطة — المركز المفوّتر ومسافته ومبلغه مقابل المركز الصحيح ومسافته ومبلغه، والفرق للردة، والردود، وإجمالي الفرق، مرتّبة بالأثر المالي. مع تصدير Excel بنفس الأعمدة والترتيب.
- **1.8.0** — اعتماد فاتورة أبريل 2026 الحقيقية (ملف .xlsb) كبيانات افتراضية بدل بيانات العرض: 42,432 ردة، هدر 2,476,214 ر.س، التزام 67%، 1283 محطة — مع كل التصحيحات (نجران محذوفة، بيش مصحّحة، المحطات الناقصة مسمّاة، المدن مصحّحة).
- **1.7.7** — تسمية المحطات «الناقصة» (خارج المرجع) باسمها من الفاتورة بدل تركها فارغة. مثال: المحطة رقم 10 مصدرها وجهة «010 - HAREEQ DEFENCE» في الفاتورة، وكانت تظهر بلا اسم — الآن تظهر باسمها. خفّض المحطات المجهولة من ~97 إلى 9.
- **1.7.6** — إضافة عمودَي «مركز الانطلاق (الفاتورة)» و«كل مراكز الانطلاق» إلى تصدير بيان المحطات (الأصل الفعلي من الفاتورة).
- **1.7.5** — ترتيب أعمدة المراكز الـ21 (بعد عمود التوصية) من الأقرب إلى الأبعد لكل محطة، وكل خلية تعرض «المركز (كم)».
- **1.7.4** — إضافة أعمدة المراكز الـ21 لتصدير «بيان المحطات» أيضاً.
- **1.7.3** — إضافة أعمدة مسافة الطريق من كل محطة إلى جميع المراكز الـ21 في تصدير تقرير التدقيق وتصدير الفترة (لمقارنة الأقرب يدوياً).
- **1.7.2** — تصدير كملف Excel حقيقي (.xlsx) بدل CSV: أعمدة منفصلة نظيفة تلقائياً، دعم عربي واتجاه RTL، وأرقام حقيقية — يحلّ مشكلة حشر كل البيانات في عمود واحد على Excel العربي. يشمل كل التصديرات (التقارير، التنبيهات، المحطات، الحمولة، القياس).
- **1.7.1** — تعيين المدير الافتراضي: اسم المستخدم `aldrees` وكلمة المرور `Aldrees@2026`، مع ترحيل تلقائي يُرقّي الحساب الافتراضي القديم إليه دون فقد بيانات.
- **1.7.0** — الرجوع لتسجيل الدخول بـ **اسم المستخدم** بدل البريد الإلكتروني، مع الحفاظ على نظام المستخدمين والصلاحيات والحضور كما هو. المدير الافتراضي: `aldrees` / `Aldrees@2026`. (مزوّد Supabase يحوّل اسم المستخدم داخلياً إلى بريد ثابت.)
- **1.6.0** — تصحيح الأخطاء وفق السجل الرسمي: ربط أصول «DAM PSD» تلقائياً بأرامكو الظهران (كانت تظهر كمركز خاطئ)، تصحيح اسم محطة + 42 مدينة من سجل المحطات (معظمها محطات خميس مشيط كانت مدينتها خطأ). تحقّق أن جدول التسعير مطابق لورقة «العقد» في الفاتورة (لا تغيير). المحطات الناقصة تبقى كما هي.
- **1.5.0** — إزالة «نجران» بالكامل من كل شيء (تُسقَط ردودها ولا تُعتمد كمركز مرجعي، ومحذوفة من الخريطة والبيانات). تصدير أشمل لتقرير التدقيق والفترات: يشمل المنطقة، الحالة (مدرجة/ناقصة)، مركز الانطلاق الرئيسي، وكل مراكز الانطلاق مع عدد ردود كلٍّ، والتصنيف.
- **1.4.0** — مزوّد Supabase كامل (يتفعّل تلقائياً عند ضبط مفاتيح البيئة): مصادقة حقيقية، **حضور «متصل الآن» حيّ عبر الأجهزة**، سجل دخول/خروج مركزي، وإنشاء المستخدمين عبر دالة Netlify آمنة تحمل مفتاح service_role. يعود للوضع المحلّي تلقائياً إن لم تُضبط المفاتيح.
- **1.3.0** — نظام إدارة المستخدمين والصلاحيات: المدير يضيف/يعدّل/يحذف مستخدمين (اسم، منصب، قسم، دور، صلاحية عرض/تصدير)، حضور «متصل الآن»، وأوقات الدخول والخروج. بوابة دخول بالبريد وكلمة السر، وإخفاء أدوات التصدير عن مستخدمي «العرض فقط». يعمل الآن بمزوّد محلّي، وجاهز للتحويل إلى Supabase (انظر `SUPABASE_SETUP.md`) لتفعيل الحضور الحقيقي عبر الأجهزة. المدير الافتراضي: `admin@aldrees.sa` / `Admin@1234`.
- **1.2.1** — زر حذف لكل فترة في المقارنة الشهرية (لإزالة الصفوف المكرّرة أو القديمة مثل «الشهر الجديد»).
- **1.2.0** — إصلاح ربط مركز «أرامكو بيش» (كان يظهر ضمن جازان)، قراءة أسرع للفواتير (ورقة واحدة بوضع dense)، اكتشاف الشهر تلقائياً من اسم الملف، مبدّل للتنقّل بين الفواتير المرفوعة (يحدّث كل الصفحات)، وتصدير تقرير أي فترة من المقارنة الشهرية.
- **1.1.0** — صفحة «خطة العمل حسب الفترات» (خطة معالجة مرحلية بأثر مالي + مسار أداء عبر الأشهر)، أيقونات SVG احترافية بدل الإيموجي، تحسين الخطوط ومعالجة تداخل العناوين، تفاعلات وانتقالات للواجهة، وإظهار رقم الإصدار.
- **1.0.0** — إعادة هيكلة الموقع من ملف واحد إلى مشروع Vite منظّم.

## النشر

البناء مهيّأ لـ Netlify (`netlify.toml`): الأمر `npm run build` والنشر من `dist/`.
